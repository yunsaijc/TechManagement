# 📋 智能分组与专家匹配服务概述

## 服务定位

智能分组与专家匹配服务是科技管理系统的核心组件之一。当前重点是：对申报项目做语义分组，并为后续专家匹配提供稳定、可解释的分组结果。

### 核心能力

1. **代码优先的智能分组**：以学科代码为主线分桶，再结合项目名称与简介做语义拆分
2. **全局最优专家匹配**：以评审组为单元，确保每组有足够的懂行专家
3. **智能关系回避**：自动检测并回避师生、历史合作等关系

### 解决的核心问题

| 现状痛点 | 解决方案 |
|----------|----------|
| 旧方案要么把学科代码当硬约束，要么过度脱离代码 | 采用“学科代码优先、语义二次拆分”的折中方案 |
| 同代码项目主题可能不同 | 允许同代码拆分，但仍保持代码主线 |
| 分组结果难以解释 | 每个组和每个项目都输出理由 |
| 师生关系、利益冲突 | 智能回避关系检测 |

---

## 功能特性

### 1. 智能分组 (Grouping)

- **代码优先分组**：先按学科代码体系和层级做粗分
- **语义辅助拆分**：以 `xmmc` 为主、`xmjj` 为辅，对大桶做二次拆分
- **有限使用 LLM**：仅在需要命名、复核或补充解释时使用 LLM
- **容量控制**：对过大组拆分，对过小组逐项目分散归并
- **可解释输出**：记录每个组和每个项目的归组理由与置信度

### 2. 专家匹配 (Matching)

- **专家画像构建**：从研究领域、论文、擅长专业构建专家画像
- **全局最优匹配**：以组为单元的整体优化匹配
- **关系回避**：支持师生、历史合作、单位回避
- **可解释性**：匹配结果附带原因说明

---

## 业务流程

```
┌─────────────────────────────────────────────────────────────────┐
│                     智能分组与匹配流程                          │
└─────────────────────────────────────────────────────────────────┘

     用户请求 (year, limit)
         │
         ▼
┌─────────────────────┐
│   参数校验与预处理   │
└──────┬──────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────────────┐
│                     分组子服务 (Grouping)                       │
├─────────────────────────────────────────────────────────────────┤
│  1. 获取项目名称 + 可选简介 + 学科代码                           │
│  2. 按学科代码体系与层级做粗分                                   │
│  3. 仅对超大桶做 embedding / 语义拆分                            │
│  4. 对过小组逐项目重分配，对过大组拆分                           │
│  5. 按需生成组标题与解释                                         │
│  6. 做代码体系隔离检查                                          │
└────────┬────────────────────────────────────────────────────────┘
         │
         ▼
┌─────────────────────┐     ┌─────────────────────┐
│    分组结果         │     │    专家匹配子服务    │
│ - 主题组             │     │    (Matching)       │
│ - 每组理由           │     │                     │
│ - 复核标记           │     │ 1. 专家画像构建    │
└─────────────────────┘     │ 2. 向量相似度计算   │
                            │ 3. 全局最优匹配     │
                            │ 4. 关系回避检测     │
                            └──────────┬──────────┘
                                      │
                            ┌─────────┴─────────┐
                            │    结果聚合       │
                            │ - 分组结果        │
                            │ - 匹配结果        │
                            │ - 可解释报告      │
                            └────────┬─────────┘
                                      │
                                      ▼
                              审查结果输出
```

---

## 模块结构

```
src/
├── services/
│   └── grouping/
│       ├── service.py               # 服务入口
│       ├── config.py                # 配置
│       │
│       ├── grouping/
│       │   ├── agent.py            # 分组 Agent (主流程)
│       │   ├── analyzer.py         # 项目主题分析 (LLM)
│       │   ├── regroup.py          # 语义重分组与容量修正
│       │   └── optimizer.py        # 分组优化算法
│       │
│       ├── matching/
│       │   ├── agent.py            # 匹配 Agent
│       │   ├── profiler.py         # 专家画像构建
│       │   ├── scorer.py           # 匹配度计算
│       │   ├── optimizer.py        # 全局最优匹配
│       │   └── avoidance.py        # 关系回避
│       │
│       └── storage/
│           ├── project_repo.py     # 项目数据访问
│           └── expert_repo.py      # 专家数据访问
│
├── common/
│   ├── models/
│   │   └── grouping.py            # 分组相关模型
│   ├── llm/                       # 复用现有 LLM
│   └── database/
│       └── repositories/
│           └── expert_repo.py     # 包含 SubjectRepository
```

---

## 数据来源

### 项目侧数据

| 数据库表 | 字段 | 记录数 | 用途 |
|----------|------|--------|------|
| `Sb_Jbxx` | `xmmc` (项目名称) | ~147,464 | 项目标识 |
| `Sb_Jbxx` | `gjc` (关键词) | - | 主题提取辅助 |
| `Sb_Jbxx` | `ssxk1/2` (学科代码) | - | 分组主参考信息 |
| `Sb_Sbzt` | `gkAudit` | - | 测试集审核状态过滤 |
| **`Sb_Jj`** | **`xmjj`** (项目简介) | **117,478** | **可选语义补充** |
| `Sb_Jj` | `lxbj` | 103,202 | 补充内容 |

### 分组测试数据集

当前默认测试集为项目评审库中的一份固定业务子集，特点如下：

- 数据来自 `Sb_Jbxx` 与 `Sb_Sbzt` 的关联结果
- 仅保留同一业务批次下审核通过的项目
- 用于分组调试、回归测试和策略对比
- 文档中不记录真实批次标识与敏感业务条件

### 学科代码表

学科代码来自项目评审数据库 `kjjhxm_wlps.sys_xkfl` 和自然科学奖体系 `sys_xkfl_zrjj`。在当前分组实现中，学科代码用于粗分和层级相似性判断，但不会被当作绝对硬约束。

| 数据库表 | 字段 | 用途 |
|----------|------|------|
| `kjjhxm_wlps.sys_xkfl` | `code` | 7位数字代码体系 |
| `kjjhxm_wlps.sys_xkfl` | `name` | 学科名称 |
| `kjjhxm_wlps.sys_xkfl_zrjj` | `code` | 字母开头代码体系 |
| `kjjhxm_wlps.sys_xkfl_zrjj` | `name` | 学科名称 |

### 专家侧数据

| 数据库表 | 字段 | 用途 |
|----------|------|------|
| `ZJK_ZJXX` | `SXXK1-5` (熟悉学科) | 学科匹配 |
| `ZJK_ZJXX` | `SXZY` (擅长专业) | 领域匹配 |
| `ZJK_ZJXX` | `YJLY` (研究领域) | 核心分析内容 |
| `ZJK_ZJXX` | `LWLZ` (论文论著) | 研究方向推断 |

---

## 技术选型

| 技术 | 用途 |
|------|------|
| **LLM** | 项目语义理解、分组决策、结果解释 |
| **Embedding** | 候选组召回、语义相似度筛选 |
| **聚类/优化算法** | 初分组、容量控制、重排 |
| **LangChain LCEL** | Agent 编排、链式调用 |
| **scipy** | 约束优化、线性规划 (专家匹配) |

---

## 分组参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `min_per_group` | 5 | 小组下限，低于则继续归并 |
| `max_per_group` | 15 | 每组上限，超过则拆分 |
| `top_k_candidates` | 3 | 每个项目保留的候选组数 |
| `llm_batch_size` | 20 | 单次 LLM 处理项目数 |
| `confidence_threshold` | 0.65 | 低于此值的项目标记复核 |
| `enable_embedding` | true | 是否启用 embedding 召回 |
| `enable_llm` | true | 是否启用 LLM 复核 |

> 当前实现建议优先走“学科粗分 → 仅对超大桶做 embedding/语义拆分”的快速路径。

---

## API 接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/v1/grouping/projects` | POST | 项目语义分组 |
| `/api/v1/grouping/match` | POST | 专家匹配 |
| `/api/v1/grouping/full` | POST | 一次性完成分组和匹配 |

---

## 下游文档

- [分组子服务设计](02-grouping.md)
- [专家匹配子服务设计](03-matching.md)
- [数据模型](04-models.md)
- [API 接口文档](05-api.md)
